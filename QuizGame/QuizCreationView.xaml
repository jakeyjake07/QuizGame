<UserControl x:Class="QuizGame.QuizCreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizGame"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Quiz Title: " FontSize="16" FontWeight="Bold"/>
            <TextBox Text="{Binding QuizTitle, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Margin="0,5,0,0" Padding="5"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Add New Question" Margin="0,0,0,20">
            <StackPanel Margin="10">
                <TextBlock Text="Question: " FontWeight="Bold"/>
                <TextBox Text="{Binding NewQuestionStatement}" Margin="0,5,0,10" Padding="5" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

                <TextBlock Text="Question Image (Optional):" FontWeight="Bold" Margin="0,10,0,5"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="Select Image" Click="SelectImage_Click" Padding="10,5" Margin="0,0,10,0"/>
                    <Button Content="Clear Image" Click="ClearImage_Click" Padding="10,5" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding NewQuestionImagePath}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="200"/>
                </StackPanel>

                


                <TextBlock Text="Category:" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox Text="{Binding NewQuestionCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Padding="5"/>
                
                
                
                
                

                <TextBlock Text="Answers:" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBox Text="{Binding NewQuestionAnswers[0]}" Margin="0,0,0,5" Padding="5" Tag="Answer 1"/>
                <TextBox Text="{Binding NewQuestionAnswers[1]}" Margin="0,0,0,5" Padding="5" Tag="Answer 2"/>
                <TextBox Text="{Binding NewQuestionAnswers[2]}" Margin="0,0,0,10" Padding="5" Tag="Answer 3"/>
                <TextBox Text="{Binding NewQuestionAnswers[3]}" Margin="0,0,0,10" Padding="5" Tag="Answer 4"/>

                <TextBlock Text="Correct Answer: " FontWeight="Bold"/>
                <ComboBox SelectedIndex="{Binding CorrectAnswerIndex}" Margin="0,5,0,10">
                    <ComboBoxItem Content="First Answer"/>
                    <ComboBoxItem Content="Second Answer"/>
                    <ComboBoxItem Content="Third Answer"/>
                    <ComboBoxItem Content="Fourth Answer"/>
                </ComboBox>
                
                <Button Content="Add Question" Click="AddQuestion_Click" Padding="10,5"/>

            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Questions in Quiz">
            <ListView ItemsSource="{Binding CurrentQuiz.Questions}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding Statement}" FontWeight="Bold" TextWrapping="Wrap"/>
                            
                            
                            
                            <TextBlock Text="{Binding Answers[0]}" Foreground="Green" Margin="10,2,0,0"/>
                            <TextBlock Text="{Binding Answers[1]}" Margin="10,2,0,0"/>
                            <TextBlock Text="{Binding Answers[2]}" Margin="10,2,0,0"/>
                            <TextBlock Text="{Binding Answers[3]}" Margin="10,2,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Save Quiz" Click="SaveQuiz_Click" Padding="15,5" Margin="0,0,10,0"/>
            <Button Content="Back to Menu" Click="BackButton_Click" Padding="15,5"/>
        </StackPanel>

    </Grid>
</UserControl>
