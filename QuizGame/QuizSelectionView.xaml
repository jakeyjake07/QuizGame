<UserControl x:Class="QuizGame.QuizSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizGame"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <TextBlock Text="Select a Quiz to Play" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <ListView x:Name="QuizzesListView" ItemsSource="{Binding AvailableQuizzes}" Height="200" Width="300">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="16" Padding="5"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Content="Play Selected Quiz" Click="PlayButton_Click" Margin="0,20,0,10" Padding="10,5"/>
            <Button Content="Play by Category" Click="PlayByCategory_Click" Margin="0,10" Padding="10,5"/>
            <Button Content="Create New Quiz" Click="CreateButton_Click" Margin="0,10" Padding="10,5"/>
            <Button Content="Edit Quiz" Click="EditButton_Click" Margin="0,10" Padding="10,5"/>
            <Button Content="Delete Quiz" Click="DeleteButton_Click" Margin="0,10" Padding="10,5" Background="LightCoral"/>

        </StackPanel>
    </Grid>
</UserControl>
